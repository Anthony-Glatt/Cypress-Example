/// <reference types="cypress" />

describe('Page Links', () => {
  beforeEach(() => {
    cy.visit(Cypress.config('baseUrl'));
  });
  context('When clicked', () => {
    it('About Us redirects to About Us page', () => {
      
    })
  });
});
